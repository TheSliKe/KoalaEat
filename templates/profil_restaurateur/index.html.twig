{% extends 'base.html.twig' %}

{% block title %}Profil Restaurateur{% endblock %}

{% block customstylesheets %}
    <style>
        body {
            background-image: url('../images/backRest.jpg');
            background-size: cover;
        }

    </style>
{% endblock %}

{% block navbaritems %}

    <div class="navbar-nav">
        <a class="nav-link active" href="/dashboard/restaurateur">Dashboard</a>
        <a class="nav-link" href="/profil/restaurateur">Profil</a>
    </div>

    <form>
        <button class="btn me-2" type="button" onclick='window.location.href = "/logout"' style="background-color: #9d3822; color: #FFFFFF">Déconnexion</button>
    </form>

{% endblock %}

{% block body %}




<div class="card mx-auto" style="width: 90rem; margin-top : 15px">
  <div class="card-body">
    <h5 class="card-title">Profil du Restaurateur</h5>
        <div class="container">
            <div class="row">

                <div class="col">
     
                    {{ form_start(ProfilRestaurateurForm) }}
                        {{ form_row(ProfilRestaurateurForm.RES_Nom, { label : "Votre nom" }) }}
                        {{ form_row(ProfilRestaurateurForm.RES_Prenom, { label : "Votre prénom:" }) }}
                        {{ form_row(ProfilRestaurateurForm.RES_Mail, { label : "Votre mail" }) }}
                        {{ form_row(ProfilRestaurateurForm.RES_Telephone, { label : "Numéro de téléphone" }) }}
                        {{ form_row(ProfilRestaurateurForm.RES_adresse, { label : "Adresse" }) }}
                        
                        <button type="submit" class="btn btn-dark">Sauvegarder</button>
                    {{ form_end(ProfilRestaurateurForm) }}

                </div>
                <div class="col">
                </div>

            </div>
        </div>
  </div>
</div>


<div class="card mx-auto" style="width: 90rem; margin-top : 15px">
    <div class="card-body">
        <ul class="nav nav-pills justify-content-center">

            {% for restaurant in listeRestaurant %}
                <li class="nav-item">
                    <a href="#{{ restaurant.getId() }}" class="nav-link active" data-bs-toggle="tab" style="background-color: #212529">{{ restaurant.getRELibelle() }}</a>
                </li>
            {% endfor %}

            
            <li class="nav-item">
                <a href="/" class="nav-link" style="color: #212529">Ajouter un restaurant</a>
            </li>
        
        </ul>

        <div class="tab-content">

            {% for restaurant in listeRestaurant %}
                <div class="tab-pane fade show active" id="{{ restaurant.getId() }}">
                
                    <div class="container">
                        <div class="row">
                            <div class="col">

                                <table class="table">
                                    <thead>
                                        <tr>
                                            <th scope="col">Jours</th>
                                            <th scope="col">Horaire Midi</th>
                                            <th scope="col">Horaire Soir</th>
                                        </tr>
                                    </thead>
                                    <tbody>

                                        {% for horaireResto in restaurant.getHoraireRestaurants() %}

                                            <tr>
                                                <th scope="row">{{ horaireResto.getFKSEM().getSEMLibelle }}</th>
                                                <td>{{ horaireResto.getHoraireDebutMidi()|format_datetime('none', 'short', locale='fr') ~"-"~ horaireResto.getHoraireFinMidi()|format_datetime('none', 'short', locale='fr') }}</td>
                                                <td>{{ horaireResto.getHoraireDebutSoir()|format_datetime('none', 'short', locale='fr') ~"-"~ horaireResto.getHoraireFinSoir()|format_datetime('none', 'short', locale='fr') }}</td>
                                            </tr>

                                        {% endfor %}

                                    </tbody>
                                </table>

                            </div>
                            <div class="col">

                                <div class="card mx-auto mt-3" style="width: 25rem;">
                                    <div class="card-body d-flex flex-column">
                                        <h5 class="card-title">Restaurant : {{ restaurant.getRELibelle() }}</h5>
                                        <p class="card-text">
                                        
                                        adresse : {{ restaurant.getREAdresse() }}

                                        </p>

                                        <ul class="list-group">
                                            {% for categorieResto in restaurant.getCategories() %}

                                                <li class="list-group-item">{{ categorieResto.getCALibelle() }}</li>
                                                
                                            {% endfor %}
                                        </ul>

                                        <button type="button" class="btn btn-dark mt-3">Modifier le restaurant</button>
                                    </div>
                                </div>

                            </div>
                        </div>
                    </div>

                </div>
            {% endfor %}

        </div>
    </div>
</div>

    <!--

 {{ form_start(CreerRestaurantType) }}
                        {{form_row(CreerRestaurantType.RE_Libelle, { label : "Nom du restaurant" })}}        
                        {{form_row(CreerRestaurantType.RE_Adresse, { label : "Adresse du restaurant" })}}
                        {{form_row(CreerRestaurantType.FK_VI_id, { label : "Ville" })}}
                        {{form_row(CreerRestaurantType.categories, { label : "Categories" })}}
                        <button type="submit" class="btn btn-dark">Sauvegarder</button>
                    {{ form_end(CreerRestaurantType) }}

   
-->
{% endblock %}
